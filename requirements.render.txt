# ============================================================
# Production requirements for Render deployment.
# Heavy ML packages (sentence-transformers, spaCy, transformers,
# scikit-learn, etc.) are intentionally excluded — they run
# only on the local desktop app, not on this API server.
# For local full development use requirements.txt instead.
# ============================================================

# Core API framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
orjson>=3.9.0

# Databases
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
sqlalchemy[asyncio]>=2.0.25
alembic>=1.13.1
neo4j>=5.14.0
redis>=5.0.1
qdrant-client>=1.7.0

# Auth & security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
pyotp>=2.9.0
qrcode>=7.4.2
slowapi>=0.1.9

# Settings & config
pydantic[email]>=2.5.3
pydantic-settings>=2.2.1
python-dotenv>=1.0.0

# HTTP & async
httpx>=0.27.0

# AI/LLM (lightweight, no local model download)
openai>=1.0.0
anthropic>=0.18.0
ollama>=0.1.0

# Billing
stripe>=8.0.0

# Logging & monitoring
structlog>=23.1.0
sentry-sdk>=1.39.1
prometheus-client>=0.19.0
apscheduler>=3.10.0

# Cloud sync integrations
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.1.0
msal>=1.25.0

# Templates / email
jinja2>=3.1.2
markdown>=3.5.2
